package expression.exceptions;

import expression.AllExpression;
import expression.Count;

public class CheckedCount extends Count{
    public CheckedCount(AllExpression el) {
        super(el);
    }
}
